import { CriticalException } from "@/components/CriticalException";

/** Screen 01 — the operational problem, its deadline, and one way forward. */
export default function Home() {
  return <CriticalException />;
}
